version: "3.8"
services:
  web:
    build: .
    container_name: scda_web
    environment:
      - BASE_MODEL=microsoft/Phi-3-mini-4k-instruct
      - ADAPTER_DIR=models/phi3-mini-lora
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
      - "7860:7860"
    deploy:
      resources:
        limits:
          memory: 6G
